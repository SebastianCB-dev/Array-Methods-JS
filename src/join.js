console.clear();
const countryList = ['England', 'Canada', 'Colombia'];

// Join method creates a string with the elements from an array.
// Comma is the separate by default

const res = countryList.join(', ')

console.log('I want to visit:', res);