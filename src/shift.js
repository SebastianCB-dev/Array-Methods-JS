console.clear();
const numbers = [1, 2, 3];

// Delete the first element of an array -> Opossite of pop

const deletedElement = numbers.shift();

console.log('Array:', numbers);
console.log('Deleted Element:', deletedElement);