console.clear();
const numbers = [1, 2, 3];

// Add the new elements at the beggining of the array.

const newLengthArray = numbers.unshift(0, -1, -2, -3);

console.log('Array:', numbers);
console.log('New Length Array:', newLengthArray);